void (*my_func)(int a, char b);

struct {
  int a, b;
} my_anon_struct[10];

int c;

---

cdef extern from "globals.test":

    void (*)(int, char) my_func

    cdef struct struct (unnamed at globals.test:3:1):
        int a
        int b

    struct (unnamed at globals.test:3:1) my_anon_struct[10]

    int c
